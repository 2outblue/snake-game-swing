package com.components.menu;

// TODO: is this needed? maybe remove this and use directly the BaseMenuButton (make it non-abstract)
public class MenuButton extends BaseMenuButton {
    public MenuButton(String path) {
        super(path);
    }

}
